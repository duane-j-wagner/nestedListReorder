import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.expander',
  appName: 'expander',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
